<div class="container d-flex flex-wrap">
  
  <% gossip.tags.each do |tag| %>
    <span class="d-inline-block rounded-pill px-2 py-1 text-bg-light">
    <%= link_to tag.title, tag_path(tag.id)  %></span>
     
  <% end %>
</div>
