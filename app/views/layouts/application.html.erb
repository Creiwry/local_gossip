<!DOCTYPE html>
<html>
  <head>
    <title>TheGossipProject</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid d-flex justify-content-between">
      <a class="navbar-brand" href="#">The Gossip Project</a>
      <div class="navbar-nav w-50 flex-row justify-content-around me-auto mb-2 mb-lg-0">
        <div class="nav-item">
          <%= link_to 'Home', root_path, class:'nav-link' %>
        </div>
        <div class="nav-item">
          <%= link_to 'Contacts', contacts_path, class:'nav-link' %>
        </div>
        <div class="nav-item">
          <%= link_to 'Team', team_path, class:'nav-link' %>
        </div>
      </div>
    </div>
  </nav>
  <body>
    <%= yield %>
  </body>

</html>
